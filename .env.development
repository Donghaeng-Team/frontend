# API 설정 - 개발 환경에서는 Vite proxy 사용 (빈 문자열로 설정)
# vite.config.ts의 proxy 설정이 /api, /internal, /auth 요청을 localhost:8080으로 전달
# 빈 문자열이면 상대 경로로 요청 → Vite proxy가 처리 → CORS 우회
VITE_API_BASE_URL=

# 환경 구분
VITE_NODE_ENV=development

# API 타임아웃 (밀리초)
VITE_API_TIMEOUT=10000

# OAuth 관련 (필요시)
# VITE_KAKAO_CLIENT_ID=your_kakao_client_id

# 디버그 모드
VITE_DEBUG=true